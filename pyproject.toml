[project]
name = "bigdata-lab-ece"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "p-vandergucht-ece", email = "pierre.vandergucht@edu.ece.fr" }
]
requires-python = ">=3.13"
dependencies = [
    "faker>=40.37.0",
]

[project.scripts]
dataset-users = "bigdata_lab_ece.dataset_users:main"
dataset-orders = "bigdata_lab_ece.dataset_orders:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
